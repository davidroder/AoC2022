# Solutions for Advent of Code 2022 in Haskell

Uses a custom [preprocessor](discover/discover.hs) to discover solutions. This must be compiled to `discover/discover.exe` before `Main` can be compiled.